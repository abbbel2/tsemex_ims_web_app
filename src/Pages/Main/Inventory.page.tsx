import InventoryContainer from "../../Container/Inventory/Inventory.container";
import MainLayout from "../Layout/Main.layout";

const InventoryPage = () => {
  return (
    <MainLayout>
      <InventoryContainer />
    </MainLayout>
  );
};

export default InventoryPage;
